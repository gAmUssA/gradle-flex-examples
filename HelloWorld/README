To build this Flex application with Gradle, open a command prompt in this directory and type:

gradle -i build

Gradle will compile the Flex application and publish the SWF file to the build directory.

